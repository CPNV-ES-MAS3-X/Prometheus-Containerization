---
- hosts: srv-prometheus-01
  
  become: true
  
  tasks:

#    - name: Install Docker Engine
#      apt:
#        name: docker.io
#        state: present
    - name: Install Docker CLI
      apt:
        name: docker-compose
        state: present

    - name: Start Docker service
      service:
        name: docker
        state: started
        enabled: true

    - name: Pull nginx Docker image
      docker_image:
        name: nginx
        source: pull

    - name: Create volume
      docker_volume:
        name: nginx-data
        driver: local

    - name: Create volume
      docker_volume:
        name: nginx-conf
        driver: local

    - name: copy config file 
      template:
        src: files/index.html
        dest: "/var/lib/docker/volumes/nginx-data"

    - name: copy config file 
      template:
        src: files/nginx.conf
        dest: "/var/lib/docker/volumes/nginx-conf/"

    - name: Run nginx Docker container
      docker_container:
        name: nginx
        image: nginx
        state: started
        ports:
          - "80:80"
        volumes:
          - "nginx-data:/usr/share/nginx/html:ro"
          - "/var/lib/docker/volumes/nginx-conf/nginx.conf:/etc/nginx/nginx.conf:ro"

    - name: Copy a file into the container
      community.docker.docker_container_copy_into:
        container: nginx
        path: files/nginx.conf
        container_path: /etc/nginx/nginx.conf